
<div>
{{#each article}}
    {{#unless this.boolean}}
<p>{{this.title}}</p>
<a href="{{this.link}}"> {{this.link}}</a>
<button id="{{this._id}}" value="{{this.boolean}}" class="save">Save</button>
<button>Add Note</button>
<button>Delete Note</button>
    {{/unless}}
{{/each}}

</div>